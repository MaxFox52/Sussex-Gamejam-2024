using UnityEngine;

public class TestScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // commit from mac grrrr
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
